---
import logoG from "../../assets/logo_g.PNG";
import darkShowcaseAsset from "../../assets/dark.webp";
import lightShowcaseAsset from "../../assets/light.webp";
import Layout from "../layouts/Layout.astro";

const showcaseSizes = "(max-width: 1200px) 96vw, 1100px";
const showcaseWidth = lightShowcaseAsset.width;
const showcaseHeight = lightShowcaseAsset.height;

const features = [
	{
		num: "01",
		cat: "EDITOR",
		title: "Markdown-native.",
		desc: "A distraction-free editor with a floating toolbar, slash commands, and live preview. Every note is a plain .md file you own forever.",
	},
	{
		num: "02",
		cat: "CONNECTIONS",
		title: "Wikilinks & backlinks.",
		desc: "Connect any idea to any other with [[wikilinks]]. See what links back. Your notes stay linked together — no folders, no silos.",
	},
	{
		num: "03",
		cat: "INTELLIGENCE",
		title: "AI that knows you.",
		desc: "Ask questions about your own notes. Summarize, compare, or draft — powered by your existing ChatGPT subscription (Plus, Team, or Pro), Anthropic, or a local model you control.",
	},
	{
		num: "04",
		cat: "PRODUCTIVITY",
		title: "Tasks, tables & boards.",
		desc: "Drop a checkbox in any note. Organize with tables and kanban boards. Glyph pulls tasks into Inbox, Today, and Upcoming — no separate app needed.",
	},
	{
		num: "05",
		cat: "SEARCH",
		title: "Find anything, instantly.",
		desc: "Hit ⌘P and find anything — by tag, folder, or full-text. Results appear before you finish typing.",
	},
	{
		num: "06",
		cat: "CUSTOMIZATION",
		title: "Make it yours.",
		desc: "Accent colors, custom fonts, daily notes, and shortcuts — bend Glyph to match how you think.",
	},
];

const faqs = [
	{
		q: "Is there a subscription?",
		a: "No. Glyph is a one-time purchase with lifetime access. No recurring fees, no seat limits, no device binding.",
	},
	{
		q: "Do you collect my data?",
		a: "Never. Your notes stay on your Mac as plain markdown files. Nothing is uploaded, tracked, or phoned home. Ever.",
	},
	{
		q: "Can I try it before buying?",
		a: 'Yes. Glyph comes with a 48-hour free trial. No credit card required — just <a href="https://github.com/SidhuK/Glyph/releases" target="_blank" rel="noopener" style="text-decoration:underline">download the latest version</a> and start writing.',
	},
	{
		q: "Is it open source?",
		a: "Yes. The source code is public on GitHub. Official release binaries are licensed, but you can always build from source.",
	},
	{
		q: "Is a Windows version planned?",
		a: "Eventually, yes. Glyph is built with cross-platform technology, so Windows and Linux are absolutely possible. Right now, all our energy goes into making the Mac experience exceptional.",
	},
];
---

<Layout
	preloadImage={lightShowcaseAsset.src}
	preloadImageSizes={showcaseSizes}
>
	<div class="page">

		<!-- ── HEADER ── -->
		<header class="header">
			<a href="/" class="logo">
				<img src={logoG.src} alt="" class="logo-icon" />
				<span>GLYPH</span>
				<span class="logo-version">EARLY ACCESS</span>
			</a>
			<nav class="nav">
				<a href="https://github.com/SidhuK/Glyph" target="_blank" rel="noopener">GitHub</a>
				<a href="https://github.com/SidhuK/Glyph/releases" target="_blank" rel="noopener">Changelog</a>
			</nav>
			<a href="https://karatsidhu.gumroad.com/l/sqxfay" class="header-cta" target="_blank" rel="noopener">Get Glyph ↗</a>
		</header>

		<!-- ── HERO ── -->
		<section class="hero">
			<p class="label">LOCAL-FIRST · MARKDOWN · MACOS</p>
			<h1>A quiet place for your<br />thoughts and ideas</h1>
			<p class="hero-sub">Write in markdown. Connect ideas with links. Let AI help when you ask. Everything lives on your machine — plain files, always yours.</p>
			<div class="hero-cta-stack">
				<div class="hero-actions">
					<a href="https://karatsidhu.gumroad.com/l/sqxfay" class="btn-primary" target="_blank" rel="noopener">Get Started</a>
					<a href="https://github.com/SidhuK/Glyph" class="btn-outline" target="_blank" rel="noopener">GitHub</a>
				</div>
				<div class="install-strip" aria-label="Homebrew install command">
					<div class="install-strip-bar">
						<code class="install-strip-code" data-install-command>brew install --cask SidhuK/glyph/glyph</code>
						<button class="install-strip-copy" type="button" data-copy-install aria-label="Copy Homebrew install command" title="Copy command">
							<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
								<rect x="9" y="9" width="10" height="10" rx="2"></rect>
								<path d="M5 15V7a2 2 0 0 1 2-2h8"></path>
							</svg>
						</button>
					</div>
				</div>
			</div>
		</section>

		<!-- ── SHOWCASE ── -->
		<section class="showcase">
				<div class="showcase-wrap">
					<img
						class="showcase-img"
						src={lightShowcaseAsset.src}
						sizes={showcaseSizes}
						width={showcaseWidth}
						height={showcaseHeight}
						alt="Glyph in light mode"
						loading="eager"
						decoding="async"
						fetchpriority="high"
						data-light-src={lightShowcaseAsset.src}
						data-dark-src={darkShowcaseAsset.src}
						data-light-alt="Glyph in light mode"
						data-dark-alt="Glyph in dark mode"
					/>
			</div>
			<div class="mode-toggle-wrap">
				<button
					class="mode-toggle"
					type="button"
					data-mode-toggle
					aria-label="Switch between light and dark preview"
					aria-pressed="false"
				>
					<svg class="mode-icon icon-sun" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>
					<svg class="mode-icon icon-moon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
				</button>
			</div>
		</section>

		<!-- ── FEATURES ── -->
		<section class="section" data-reveal>
			<p class="label">FEATURES</p>
			<h2>Not your average<br />notes app</h2>
			<div class="features-grid">
				{features.map((f) => (
					<div class="feature-cell">
						<p class="feature-label"><span class="feature-num">{f.num}</span> {f.cat}</p>
						<h3 class="feature-title">{f.title}</h3>
						<p class="feature-desc">{f.desc}</p>
					</div>
				))}
			</div>
		</section>

		<!-- ── LOCAL-FIRST ── -->
		<section class="section" data-reveal>
			<p class="label">PRIVACY</p>
			<h2>Yours, completely</h2>
			<p class="section-sub">Your notes never leave your machine. No account, no cloud, no telemetry. Open a folder and start writing.</p>
			<div class="privacy-grid">
				<div class="privacy-cell privacy-cell--tip">
					<p class="privacy-stat">0</p>
					<p class="privacy-label">data collected</p>
					<div class="tip-card">
						<p class="tip-title">Private by default</p>
						<ul class="tip-list">
							<li>No account required to use Glyph</li>
							<li>No telemetry, tracking, or sync layer</li>
							<li>Your notes stay available even without internet</li>
						</ul>
						<p class="tip-foot">Glyph works locally first, so privacy and offline access are built in from the start.</p>
					</div>
				</div>
				<div class="privacy-cell privacy-cell--tip">
					<p class="privacy-stat">.md</p>
					<p class="privacy-label">plain files you own</p>
					<div class="tip-card">
						<p class="tip-title">No lock-in</p>
						<ul class="tip-list">
							<li>Every note is saved as a standard markdown file</li>
							<li>Open your folder anytime in Finder or another editor</li>
							<li>Your notes stay yours, even if you stop using Glyph</li>
						</ul>
						<p class="tip-foot">Glyph organizes your notes without trapping them in a proprietary format.</p>
					</div>
				</div>
				<div class="privacy-cell privacy-cell--tip">
					<p class="privacy-stat">&lt;40<span class="privacy-unit">MB</span></p>
					<p class="privacy-label">app size</p>
					<div class="tip-card">
						<p class="tip-title">Why so small?</p>
						<ul class="tip-list">
							<li>Built natively with Tauri &amp; Rust</li>
							<li>No Electron, no bundled browser</li>
							<li>Uses under 250 MB of memory</li>
						</ul>
						<p class="tip-foot">A lean, fast app that respects your disk and your battery.</p>
					</div>
				</div>
				<div class="privacy-cell privacy-cell--tip">
					<p class="privacy-stat">BYOK</p>
					<p class="privacy-label">bring your own AI key</p>
					<div class="tip-card">
						<p class="tip-title">Supported providers</p>
						<ul class="tip-list">
							<li>OpenAI / ChatGPT (Plus, Team, Pro)</li>
							<li>Anthropic (Claude)</li>
							<li>Google Gemini</li>
							<li>OpenRouter</li>
							<li>Local models (Ollama, LM Studio)</li>
						</ul>
						<p class="tip-foot">Use your own API key — we never store or proxy it.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- ── DESIGN ── -->
		<section class="section" data-reveal>
			<p class="label">DESIGN</p>
			<h2>Obsessively crafted</h2>
			<p class="section-sub">A native macOS app built by people who sweat the details. Every pixel, every shortcut, every transition.</p>
		</section>

		<!-- ── FAQ ── -->
		<section class="section" data-reveal>
			<p class="label">FREQUENT CONCERNS</p>
			<h2>Everything you need to<br />know before you begin</h2>
			<div class="faq-list">
				{faqs.map((f) => (
					<details class="faq-item">
						<summary class="faq-summary">
							<span class="faq-q">{f.q}</span>
							<span class="faq-plus">+</span>
						</summary>
						<p class="faq-a" set:html={f.a} />
					</details>
				))}
			</div>
		</section>

		<!-- ── CTA ── -->
		<section class="section cta" data-reveal>
			<p class="label">YOUR NEXT STEP</p>
			<h2>Start writing with<br />Glyph today</h2>
			<div class="cta-actions">
				<a href="https://karatsidhu.gumroad.com/l/sqxfay" class="btn-primary" target="_blank" rel="noopener">Get Glyph</a>
				<a href="https://github.com/SidhuK/Glyph" class="btn-outline" target="_blank" rel="noopener">View Source</a>
			</div>
		</section>

		<!-- ── FOOTER ── -->
		<footer class="footer">
			<div class="footer-inner">
				<div class="footer-col">
					<a href="/" class="logo">
						<img src={logoG.src} alt="" class="logo-icon" />
						<span>GLYPH</span>
					</a>
				</div>
				<div class="footer-col">
					<p class="footer-heading">PRODUCT</p>
					<a href="https://github.com/SidhuK/Glyph/releases" target="_blank" rel="noopener">Changelog</a>
					<a href="https://github.com/SidhuK/Glyph" target="_blank" rel="noopener">GitHub</a>
				</div>
				<div class="footer-col">
					<p class="footer-heading">PURCHASE</p>
					<a href="https://karatsidhu.gumroad.com/l/sqxfay" target="_blank" rel="noopener">Gumroad</a>
				</div>
				<div class="footer-col">
					<p class="footer-heading">LEGAL</p>
					<a href="#">Terms</a>
					<a href="#">Privacy</a>
				</div>
			</div>
			<p class="footer-copy">© 2026 Glyph. All rights reserved.</p>
		</footer>

	</div>
</Layout>

<style>
	/* ── Page ── */
	.page {
		width: min(720px, 90vw);
		margin-inline: auto;
	}

	/* ── Header ── */
	.header {
		position: sticky;
		top: 0;
		z-index: 100;
		display: flex;
		align-items: center;
		padding: 0.75rem 0;
		background: transparent;
	}

	.logo {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.14em;
	}

	.logo-icon {
		width: 1.25rem;
		height: 1.25rem;
		border-radius: 4px;
	}

	.logo-version {
		font-size: 0.55rem;
		font-weight: 500;
		letter-spacing: 0.06em;
		color: var(--faint);
		border: 1px solid var(--border);
		padding: 0.1rem 0.35rem;
		border-radius: 3px;
		line-height: 1;
		margin-left: 0.1rem;
	}

	.nav {
		display: flex;
		align-items: center;
		gap: 0.2rem;
		margin-left: auto;
	}

	.nav a {
		display: flex;
		align-items: center;
		padding: 0.4rem 0.7rem;
		font-size: 0.72rem;
		font-weight: 500;
		letter-spacing: 0.06em;
		text-transform: uppercase;
		color: var(--muted);
		border-radius: 4px;
		transition: color 0.15s, background 0.15s;
	}

	.nav a:hover {
		color: var(--ink);
		background: rgba(0, 0, 0, 0.04);
	}

	.header-cta {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		margin-left: 0.6rem;
		min-height: 2.4rem;
		padding: 0.45rem 1rem;
		background: var(--ink);
		color: var(--bg);
		font-family: var(--mono);
		font-size: 0.7rem;
		font-weight: 600;
		letter-spacing: 0.03em;
		border-radius: 6px;
		border: 1px solid var(--ink);
		cursor: pointer;
		transition:
			transform 0.15s ease,
			background 0.15s ease;
	}

	.header-cta:hover {
		transform: translateY(-1px);
		background: #222;
	}

	.header-cta:focus-visible,
	.btn-primary:focus-visible,
	.btn-outline:focus-visible {
		outline: 2px solid rgba(17, 17, 17, 0.15);
		outline-offset: 2px;
	}

	.btn-primary {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		padding: 0.72rem 1.3rem;
		background: var(--ink);
		color: var(--bg);
		font-family: var(--mono);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.03em;
		border: none;
		border-right: 1px solid rgba(250, 250, 250, 0.12);
		cursor: pointer;
		transition:
			background 0.15s ease,
			color 0.15s ease;
	}

	.btn-primary:hover {
		background: #202020;
	}

	.btn-outline {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		padding: 0.72rem 1.3rem;
		background: rgba(255, 255, 255, 0.68);
		color: var(--ink);
		font-family: var(--mono);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.03em;
		border: none;
		cursor: pointer;
		transition:
			background 0.15s ease,
			color 0.15s ease;
	}

	.btn-outline:hover {
		background: rgba(17, 17, 17, 0.06);
	}

	/* ── Label ── */
	.label {
		font-family: var(--mono);
		font-size: 0.68rem;
		font-weight: 500;
		letter-spacing: 0.18em;
		color: var(--faint);
		text-align: center;
		margin-bottom: 0.8rem;
	}

	/* ── Hero ── */
	.hero {
		text-align: center;
		padding: 5rem 0 3.5rem;
	}

	h1 {
		font-family: var(--serif);
		font-size: clamp(2.4rem, 6vw, 3rem);
		font-weight: 400;
		line-height: 1.18;
		font-style: italic;
		color: var(--ink);
		margin-bottom: 1.2rem;
	}

	.hero-sub {
		font-size: 0.85rem;
		color: var(--muted);
		line-height: 1.7;
		max-width: 460px;
		margin: 0 auto 2rem;
	}

	.hero-cta-stack {
		display: inline-flex;
		flex-direction: column;
		align-items: center;
		gap: 0.75rem;
	}

	.hero-actions {
		display: inline-flex;
		align-items: stretch;
		justify-content: center;
		border: 1px solid rgba(17, 17, 17, 0.12);
		border-radius: 9px;
		overflow: hidden;
		background: rgba(255, 255, 255, 0.82);
		backdrop-filter: blur(10px);
	}

	.install-strip {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		width: 100%;
		max-width: 34rem;
	}

	.install-strip-bar {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		align-items: center;
		gap: 0.65rem;
		width: 100%;
	}

	.install-strip-code {
		display: block;
		font-family: var(--mono);
		font-size: 0.78rem;
		color: var(--ink);
		line-height: 1.45;
		min-width: 0;
		overflow-wrap: anywhere;
	}

	.install-strip-copy {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		border: 1px solid rgba(17, 17, 17, 0.12);
		border-radius: 0.6rem;
		background: transparent;
		color: var(--muted);
		cursor: pointer;
		transition: background 0.15s ease, color 0.15s ease, border-color 0.15s ease;
		justify-self: end;
	}

	.install-strip-copy.is-copied {
		background: rgba(17, 17, 17, 0.06);
		color: var(--ink);
		border-color: rgba(17, 17, 17, 0.2);
	}

	.install-strip-copy:hover {
		background: rgba(17, 17, 17, 0.05);
		color: var(--ink);
		border-color: rgba(17, 17, 17, 0.2);
	}

	.install-strip-copy:focus-visible {
		outline: 2px solid rgba(17, 17, 17, 0.15);
		outline-offset: 2px;
	}

	/* ── Showcase ── */
	.showcase {
		padding: 3.5rem 0;
		width: min(1100px, 96vw);
		margin-left: calc(-1 * (min(1100px, 96vw) - 100%) / 2);
	}

	.showcase-img {
		width: 100%;
		height: auto;
		display: block;
		transition: opacity 0.4s ease;
	}

	.mode-toggle-wrap {
		display: flex;
		justify-content: center;
		margin-top: 0.8rem;
	}

	.mode-toggle {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.3rem 0.6rem;
		background: transparent;
		border: 1px solid var(--border);
		cursor: pointer;
		transition: border-color 0.15s;
	}

	.mode-toggle:hover { border-color: var(--faint); }

	.mode-icon {
		display: block;
		color: var(--faint);
		transition: color 0.2s;
	}

	.icon-sun { color: var(--ink); }
	.showcase.is-dark .icon-sun { color: var(--faint); }
	.showcase.is-dark .icon-moon { color: var(--ink); }

	/* ── Sections ── */
	.section {
		padding: 4.5rem 0;
		text-align: center;
	}

	h2 {
		font-family: var(--serif);
		font-size: clamp(1.8rem, 5vw, 2.4rem);
		font-weight: 400;
		font-style: italic;
		line-height: 1.2;
		color: var(--ink);
		margin-bottom: 1rem;
	}

	.section-sub {
		font-size: 0.85rem;
		color: var(--muted);
		line-height: 1.7;
		max-width: 480px;
		margin: 0 auto 2.5rem;
	}

	/* ── Feature grid (3-col windowpane) ── */
	.features-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		border: 1px solid var(--border);
		margin-top: 2.5rem;
		text-align: left;
	}

	.feature-cell {
		padding: 1.5rem;
		border-bottom: 1px solid var(--border);
		border-right: 1px solid var(--border);
	}

	.feature-cell:nth-child(3n) { border-right: none; }
	.feature-cell:nth-last-child(-n+3) { border-bottom: none; }

	.feature-label {
		font-family: var(--mono);
		font-size: 0.65rem;
		font-weight: 500;
		letter-spacing: 0.12em;
		color: var(--faint);
		margin-bottom: 0.6rem;
	}

	.feature-num {
		color: var(--muted);
	}

	.feature-title {
		font-family: var(--mono);
		font-size: 0.9rem;
		font-weight: 600;
		margin-bottom: 0.6rem;
		color: var(--ink);
	}

	.feature-desc {
		font-size: 0.78rem;
		color: var(--muted);
		line-height: 1.65;
	}

	/* ── Privacy grid ── */
	.privacy-grid {
		display: flex;
		flex-wrap: wrap;
		border: 1px solid var(--border);
		text-align: center;
	}

	.privacy-cell {
		padding: 2rem 1rem;
		width: 25%;
		border-right: 1px solid var(--border);
		border-bottom: none;
	}

	.privacy-cell:last-child {
		border-right: none;
	}

	.privacy-stat {
		font-family: var(--serif);
		font-size: 2.4rem;
		font-weight: 400;
		font-style: italic;
		color: var(--ink);
		line-height: 1;
		margin-bottom: 0.5rem;
	}

	.privacy-unit {
		font-size: 1.2rem;
		font-style: normal;
		margin-left: 0.15rem;
	}

	.privacy-label {
		font-size: 0.72rem;
		font-weight: 500;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--faint);
	}

	/* ── Tooltip card ── */
	.privacy-cell--tip {
		position: relative;
		cursor: default;
	}

	.tip-card {
		position: absolute;
		top: calc(100% + 10px);
		left: 50%;
		transform: translateX(-50%) translateY(-6px);
		width: 240px;
		padding: 1rem 1.1rem;
		background: var(--bg);
		border: 1px solid var(--border);
		border-radius: 6px;
		text-align: left;
		opacity: 0;
		pointer-events: none;
		transition: opacity 0.2s ease, transform 0.2s ease;
		z-index: 50;
	}

	.tip-card::after {
		content: '';
		position: absolute;
		bottom: 100%;
		left: 50%;
		transform: translateX(-50%);
		border: 6px solid transparent;
		border-bottom-color: var(--border);
	}

	.privacy-cell--tip:hover .tip-card {
		opacity: 1;
		pointer-events: auto;
		transform: translateX(-50%) translateY(0);
	}

	.tip-title {
		font-family: var(--mono);
		font-size: 0.65rem;
		font-weight: 600;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--faint);
		margin-bottom: 0.5rem;
	}

	.tip-list {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 0.3rem;
		margin-bottom: 0.6rem;
	}

	.tip-list li {
		font-size: 0.75rem;
		color: var(--ink);
		line-height: 1.4;
	}

	.tip-list li::before {
		content: '·';
		color: var(--faint);
		margin-right: 0.4rem;
		font-weight: 700;
	}

	.tip-foot {
		font-size: 0.68rem;
		color: var(--faint);
		line-height: 1.5;
		border-top: 1px solid var(--border);
		padding-top: 0.5rem;
	}

	/* ── FAQ ── */
	.faq-list {
		text-align: left;
		margin-top: 2rem;
		border: 1px solid var(--border);
	}

	.faq-item {
		border-bottom: 1px solid var(--border);
	}

	.faq-item:last-child { border-bottom: none; }

	.faq-summary {
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 1rem 1.2rem;
		cursor: pointer;
		list-style: none;
		user-select: none;
		transition: background 0.15s;
	}

	.faq-summary:hover { background: rgba(0,0,0,0.015); }
	.faq-summary::-webkit-details-marker { display: none; }
	.faq-summary::marker { display: none; content: ''; }

	.faq-q {
		font-size: 0.85rem;
		font-weight: 500;
	}

	.faq-plus {
		font-family: var(--mono);
		font-size: 1rem;
		color: var(--faint);
		transition: transform 0.2s;
		flex-shrink: 0;
		margin-left: 1rem;
	}

	.faq-item[open] .faq-plus { transform: rotate(45deg); }

	.faq-a {
		padding: 0 1.2rem 1.2rem;
		font-size: 0.82rem;
		color: var(--muted);
		line-height: 1.7;
	}

	/* ── CTA ── */
	.cta { padding: 5rem 0; }

	.cta-actions {
		display: inline-flex;
		align-items: stretch;
		justify-content: center;
		margin-top: 1.5rem;
		border: 1px solid rgba(17, 17, 17, 0.12);
		border-radius: 9px;
		overflow: hidden;
		background: rgba(255, 255, 255, 0.82);
		backdrop-filter: blur(10px);
	}

	/* ── Footer ── */
	.footer {
		border-top: none;
		padding: 3rem 0 2rem;
	}

	.footer-inner {
		display: grid;
		grid-template-columns: 1.5fr repeat(3, 1fr);
		gap: 2rem;
		padding-bottom: 2rem;
		border-bottom: 1px solid var(--border);
	}

	.footer-col {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.footer-heading {
		font-size: 0.65rem;
		font-weight: 600;
		letter-spacing: 0.12em;
		color: var(--faint);
		margin-bottom: 0.2rem;
	}

	.footer-col a {
		font-size: 0.82rem;
		color: var(--muted);
		transition: color 0.15s;
	}

	.footer-col a:hover { color: var(--ink); }

	.footer-copy {
		margin-top: 1.5rem;
		font-size: 0.7rem;
		color: var(--faint);
		letter-spacing: 0.02em;
		text-align: center;
	}

	/* ── Reveal ── */
	:global(.reveal-pending) {
		opacity: 0;
		transform: translateY(16px);
		transition: opacity 0.5s ease, transform 0.5s ease;
	}

	:global([data-reveal].revealed) {
		opacity: 1;
		transform: none;
	}

	@media (prefers-reduced-motion: reduce) {
		:global(.reveal-pending) {
			opacity: 1;
			transform: none;
			transition: none;
		}
	}

	/* ── Responsive ── */
	@media (max-width: 640px) {
		.header { flex-wrap: wrap; }
		.nav a { padding: 0.35rem 0.5rem; font-size: 0.65rem; }
		.header-cta { padding: 0.35rem 0.7rem; font-size: 0.65rem; }

		h1 { font-size: clamp(1.8rem, 8vw, 2.4rem); }

		.hero-cta-stack,
		.hero-actions {
			display: flex;
			width: 100%;
		}

		.hero-cta-stack {
			gap: 0.65rem;
		}

		.hero-actions {
			flex-direction: column;
		}

		.install-strip {
			max-width: none;
		}

		.install-strip-code {
			font-size: 0.72rem;
		}

		.features-grid { grid-template-columns: 1fr; }
		.feature-cell { border-right: none !important; }
		.feature-cell:last-child { border-bottom: none; }

		.privacy-cell { width: 100% !important; border-right: none !important; border-bottom: 1px solid var(--border) !important; }
		.privacy-cell:last-child { border-bottom: none !important; }

		.footer-inner { grid-template-columns: 1fr 1fr; }
	}
</style>

<script>
	const defaultCopyLabel = 'Copy command';
	const copiedCopyLabel = 'Copied';
	const failedCopyLabel = 'Copy failed';

	const revealItems = Array.from(document.querySelectorAll('[data-reveal]'));
	const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	if (prefersReducedMotion || !('IntersectionObserver' in window)) {
		revealItems.forEach((el) => el.classList.add('revealed'));
	} else {
		const observer = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.remove('reveal-pending');
						entry.target.classList.add('revealed');
						observer.unobserve(entry.target);
					}
				});
			},
			{ threshold: 0.1 }
		);

		revealItems.forEach((el) => {
			const rect = el.getBoundingClientRect();
			if (rect.top <= window.innerHeight * 0.92) {
				el.classList.add('revealed');
				return;
			}

			el.classList.add('reveal-pending');
			observer.observe(el);
		});
	}

	const toggle = document.querySelector('[data-mode-toggle]');
	const showcase = toggle?.closest('.showcase');
	const showcaseImage = showcase?.querySelector('.showcase-img');
	let isDarkMode = false;

	const setShowcaseMode = (darkMode) => {
		if (!(showcaseImage instanceof HTMLImageElement)) return;

		const nextSrc = darkMode ? showcaseImage.dataset.darkSrc : showcaseImage.dataset.lightSrc;
		const nextAlt = darkMode ? showcaseImage.dataset.darkAlt : showcaseImage.dataset.lightAlt;

		if (nextSrc) showcaseImage.src = nextSrc;
		showcaseImage.removeAttribute('srcset');
		if (nextAlt) showcaseImage.alt = nextAlt;

		showcase?.classList.toggle('is-dark', darkMode);
		toggle?.setAttribute('aria-pressed', String(darkMode));
	};

	toggle?.addEventListener('click', () => {
		isDarkMode = !isDarkMode;
		setShowcaseMode(isDarkMode);
	});

	const copyButton = document.querySelector('[data-copy-install]');
	const installCommand = document.querySelector('[data-install-command]')?.textContent?.trim();
	let copyResetTimer;

	copyButton?.addEventListener('click', async () => {
		if (!installCommand) return;

		try {
			await navigator.clipboard.writeText(installCommand);
			copyButton.classList.add('is-copied');
			copyButton.setAttribute('aria-label', copiedCopyLabel);
			copyButton.setAttribute('title', copiedCopyLabel);
			window.clearTimeout(copyResetTimer);
			copyResetTimer = window.setTimeout(() => {
				copyButton.classList.remove('is-copied');
				copyButton.setAttribute('aria-label', defaultCopyLabel);
				copyButton.setAttribute('title', defaultCopyLabel);
			}, 1600);
		} catch {
			copyButton.setAttribute('aria-label', failedCopyLabel);
			copyButton.setAttribute('title', failedCopyLabel);
			window.clearTimeout(copyResetTimer);
			copyResetTimer = window.setTimeout(() => {
				copyButton.setAttribute('aria-label', defaultCopyLabel);
				copyButton.setAttribute('title', defaultCopyLabel);
			}, 1600);
		}
	});
</script>
